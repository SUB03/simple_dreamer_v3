this is simple implementation of dreamer v3 using pytorch for vector observations.

To run you need to install all requirements and run command: "pip install .".
After that you can run dreamer using this command: "python .\simple_dreamer\main.py".
Also tensorboard is included, so you can also run "tensorboard --logdir runs" 
